1. npm init -y
2. npm i express mongoose
3. npm i jsonwebtoken bcrypt cookie-parser
4. npm i multer
5. npm i debug


export DEBUG=development:* (setting the debug)
export DEBUG= (unsetting the debug)


6. npm i config
7. npm i dotenv
8. npm i connect-flash express-session


export NODE_ENV=development




1. git init
2. .gitignore
3. git add . 
4. git commit -m "initial setup"